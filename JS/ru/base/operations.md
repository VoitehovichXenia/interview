# Операции и конструкции

### Арифметические операции

- **\+ (сложение)**; **+= (сложение с присваиванием)**

  Перлы сложения:
    - сложение дробных чисел
    ```ts
    0.1 + 0.7 // 0.7999999999

    // Так происходит из-за особенности кодирования чисел в JS.
    // В JS все числа по умолчанию имеют IEEE-754 кодирование чисел и размер 64 бита

    // Смысл этого кодирования такой:
    // - любое число можно представить в экспоненциальном виде
    345.5678 // 3.455678 * (10**-2) => 3.455678e2
    // .455678 - мантисса
    // В IEEE-754 числа кодируются так
    // | знак | степень е (экпоненты) | мантисса |
    // знак - 1 бит (0 (положительное) или 1 (отрицательное))
    // степень е - 8 бит (отрицательная степень кодируется числом от 0 до 127, положительная от 128 до 255)
    // мантисса - 53 бита (двойная точность, в 32 битных числах 23 бита на мантиссу - одинарная точность)

    // При кодировании дробных чисел мантисса обрезается из-за чего и получается такой результат.

    ```
    - сложение сложных типов
    ```ts
    {} + [] // 0
    // {} может интерпретироваться как пустой блок кода, а не объект, из-за чего он просто игнорируется
    // + Number([]) => 0
    ```
- **\- (вычитание)**; **-= (вычитание с присваиванием)**
- **\* (умножение)**; ***= (умножение с присваиванием)**
- **/ (деление)**; **/= (деление с присваиванием)**
- **\*\* (возведение в степень)**
- **% (остаток от деления)**
- **-a (изменение знака)**
- **\-\- (декремент)**
```ts
let a = 2;
console.log(--a); // 1 предекремент

let b = 2;
console.log(b--); // 2 постдекремент
```
- **++ (инкремент)**
```ts
let a = 2;
console.log(++a); // 3 преинкремент

let b = 2;
console.log(b++); // 2 постинкремент
```

Все операнды преобразуются к числовому типу (за исключением "+")

### Строковые операции

```ts
let a: string = 'One'
const b: number = 1
a + b // конкатенация => 'One1'
a += b // конкатенация с присваиванием => a = 'One1' 
```

### Логические операции

- **&&** (логическое И) - возвращает true если оба операнда равны true
- **||** (логическое ИЛИ) - возвращает true если хотя бы 1 операнд true
- **!** (логическое НЕ) - инвертирует boolean значение
- **??** (оператор нулевого слияния) - возвращает значение правого операнда, если значение левого операнда содержит null или undefined, в противном случае возвращается значение левого операнда.
```ts
null ?? 1 // 1
2 ?? 1 // 2
```

### Побитовые операции

Применяются только к числам, при этом число автоматически приводится к Int32 формату

- **<< побитовый сдвиг влево** 
    сдвигает биты влево на заданную позицию
    пустое место заполняется 0
    по сути умножение на 2 в степени заданной позиции
    действует быстрее чем арифметическая операция "*"
    ```ts
    0b0000_0001 << 1 // 0b0000_0010
    // 1 => 2
    ```
- **>> арифметический сдвиг вправо**
    сдвигает биты вправо на указанную позицию
    пустое место заполняется знаковым битом
    по сути деление на 2 в степени заданной позиции
    ```ts
    0b0000_1000 >> 1 // 0b0000_0100
    // 8 => 4
    ```
- **>>> логический сдвиг вправо**
    сдвигает биты вправо на указанную позицию
    пустое место заполняется 0
    ```ts
    -56 >>> 1 // 1073741810
    ```
- **& побитовое И**
    результат - новое число где единица появляется только там, где у обоих операндов была 1
    ```ts
    0b0000_1111 & 0b0100_1100 // 0b0000_1100
    ```
- **| побитовое ИЛИ**
    результат - новое число где единица появляется там, где хотя бы у одного операнда была 1
    ```ts
    0b0000_1111 | 0b0100_1100 // 0b0100_1111
    ```
- **~ инверсия бит**
    изменяет значения бит на противоположноеб знаковый бит при этом тоже меняется
    ```ts
    ~0b0000_1111 // 0b1111_0000
    ~5 + 1 // -5
    ```

### Другие

- **?** (тернарный оператор)
```ts
1 > 2 ? 'Это true' : 'Это false' // 'Это false'
1 === 1 ? 'Это true' : 'Это false' // 'Это true'
```
- **if / else if / else** (условный оператор)
- конструкция **switch / case**
```ts
switch (x) {
  case 1: 
    console.log('Это единица');
    break;
  case 2: 
    console.log('Это двойка');
    break;
  default:
    console.log('Это что-то другое');
}
```

## Операции сравнения

### == (нестрогое равенство) и != (нестрогое неравенство)

Сравнивает только значения, приводит операндов к единому типу
Если сравниваются объект и примитив => объект приводится к примитиву

```ts
1 == '1' // true
[] == ![] // true
// ![] => boolean false
// [] == false, [] должен быть приведен к примитиву посредством toString()
// [].toString() => ''
// '' == false => true

// null и undefined равно только null и undefined и больше никому
null == undefined // true
null == 0 // false
undefined == 0 // false

// Результат сравнения с NaN всегда false
null == NaN // false
NaN == NaN // false
// Из-за особенностей кодирования IEEE-754
// NaN кодируется как
// | знак (0 или 1) | экспонента (все единицы) | мантисса (!= 0, если все 0, то это уже Infinity) |
// 1 11 11 11 11 00 00 00 00 00 00 00 00 00 00 .. 1 != 1 11 11 11 11 01 ... 0

// Ссылочные типы
[] == [] // false, т.к. ссылки разные


1 != '1' // false
```

### === (строгое равенство) и !== (строгое неравенство)

Cравнивает и типы и значения

```ts
1 === '1' // false
1 !== '1' // true

null === undefined // false
null === null // true
undefined === undefined // true

// Ссылочные типы
[1, 2] === [1, 2] // false, т.к. ссылки разные
const a = [1, 2]
const c = a;
a === c // true, т.к. в переменную скопировалась ссылка, а не само значение
```
